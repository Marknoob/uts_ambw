import 'package:flutter/material.dart';

class TopFoodieWidget extends StatelessWidget {
  const TopFoodieWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Top Foodie Page"),
    );
  }
}
